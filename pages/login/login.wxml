<view class="container">
  <view class="form">
    <input 
      placeholder="用户名" 
      data-field="username"
      bindinput="onInput"
      value="{{username}}"
    />
    <input 
      placeholder="密码" 
      password 
      data-field="password"
      bindinput="onInput"
      value="{{password}}"
    />
    <button bindtap="onLogin">登录</button>
  </view>
</view> 